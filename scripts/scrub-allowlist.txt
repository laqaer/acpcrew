# scrub-allowlist.txt — paths/patterns excluded from the scrub-lint scan.
# Each line is a grep -v pattern. Lines starting with # are comments.
# Add entries here for intentional stubs that reference internal names.

# Auth stubs (OSS placeholders for Amazon-internal auth flows)
src/junction/browser/auth.py
src/junction/browser/setup.py
src/junction/browser/screencast.py
src/junction/dashboard/handlers/mwinit.py
src/junction/dashboard/handlers/__init__.py
src/junction/dashboard/handlers_system.py:.*midway
src/junction/dashboard/handlers/messaging.py:.*Midway
src/junction/dashboard/server.py:.*midway
src/junction/dashboard/server.py:.*mwinit
src/junction/midway.py

# Brazil-aware runtime detection (checks if installed via brazil or pip)
src/junction/slack/gateway.py:.*[Bb]razil
src/junction/mcp_discovery.py:.*brazil
src/junction/transcribe.py:.*brazil

# Deny-pattern strings and security config (reference internal names as blocklist)
src/junction/dashboard/state.py:.*brazil
src/junction/config/defaults.json

# Eval test scenarios (fictional stories using internal tool names as test data)
src/junction/eval/scenarios/

# Code comments referencing provenance CRs (historical context, not leaking anything)
src/junction/agent.py:.*Amazon-specific Apollo/Brazil
src/junction/apps/backend.py:.*Brazil
src/junction/context.py:.*CR-
src/junction/eval/runner.py:.*CR-
src/junction/seed.py:.*CR-
src/junction/slack/gateway.py:.*CR-

# Frontend tests referencing brazil-build as test input
website/src/test/RunInTerminalBtn.test.tsx
website/src/test/sensitiveCommand.test.ts
# Frontend source/test comments referencing CRs for provenance
website/src/.*CR-

# Documentation (contains internal references that will be scrubbed/removed
# before publish). Anchored to the top-level ``docs/`` tree only — ``website/``
# (incl. ``website/docs/``, the shipped frontend pack-author contract) is a
# public source root and IS scanned, so a bare ``docs/`` must not swallow it.
^docs/
CHANGELOG.md

# CLAUDE.md's "do not reintroduce" policy paragraph names the internal marker
# words (Midway, mwinit, brazil, taskei, meshclaw, AIM) as the denylist itself —
# that self-documenting line must be allowed to name what it forbids.
CLAUDE.md:.*internal marker names

# AGENTS.md's matching "Never re-introduce" list, for the same reason: the rule
# forbidding an internal service has to be able to name the service. Anchored to
# the one bullet that enumerates them, so a NEW internal reference elsewhere in
# AGENTS.md is still caught.
AGENTS.md:.*device-posture tunnels

# This script and its allowlist
scripts/scrub-lint.sh
scripts/scrub-allowlist.txt

# Security path-blocklists: ``.midway`` is blocked so the agent cannot read a
# credential dir an enterprise companion may create (a security feature, kept).
website/src/utils/safePath.ts:.*\.midway
src/junction/sandbox.py:.*\.midway

# Back-compat SSO-marker default + legacy profile alias (see platform/profile.py):
# the enterprise companion's managed launcher may still use these.
src/junction/platform/profile.py:.*\.midway

# Legacy CloudFormation stack/bucket names in the migration guide — renaming
# them would make the find/delete remediation commands point at nothing.
src/junction/deploy/skills/artifact-deploy/MIGRATION.md:.*meshclaw

# Deploy scan module (detects internal content - the regex IS the feature)
src/junction/deploy/scan.py

# Public AWS documentation URLs (docs.aws.amazon.com / aws.amazon.com) — generic
# AWS product references, not internal couplings.
src/junction/cli_cloud.py:.*aws\.amazon\.com/cli
src/junction/cloud/aws.py:.*docs\.aws\.amazon\.com
src/junction/cloud/ssm.py:.*docs\.aws\.amazon\.com

# Live public AWS service class-name string the dashboard credit pill calls
# (CodeWhisperer RTS GetUsageLimits) — a functional API identifier, not a leak.
src/junction/dashboard/handlers/kiro_usage_api.py:.*codewhisperer

# Aperture feedback-collection API, the backend proxy target for the
# session-pulse survey. Despite the ``.aws.dev`` suffix (which INTERNAL_PATTERN
# matches broadly), this is a PUBLIC ingestion endpoint that external,
# non-Amazon users already submit through — the "public-api" segment in the
# hostname names that explicitly, and it was curl-verified reachable and
# functional from outside Amazon's network. Line-anchored to the two literal
# endpoint constants so a genuinely internal ``*.aws.dev`` host added elsewhere
# in this file is still caught.
src/junction/dashboard/handlers/feedback.py:.*ingestion\.aperture-public-api\.feedback\.console\.aws\.dev
src/junction/dashboard/handlers/feedback.py:.*prompt\.aperture-public-api\.feedback\.console\.aws\.dev

# code_review_sage CR-URL parser tests: synthetic CR-12345678 ids and a
# code.amazon.com sample URL are the parser's asserted test fixtures.
src/junction/apps/builtins/code_review_sage/tests/

# Deploy tests use AWS example account 123456789012 (well-known placeholder)
test/test_deploy_web_profiles.py
test/test_deploy_web_handlers.py
test/test_deploy_web_iam.py

# The ``~/.aim`` skill-discovery scan in agent.py (``_all_skill_paths`` and the
# managed-MCP path note) is OSS-inert — no such tree exists on a vanilla install
# — and carries its own TODO(aim-governance follow-up) to route through the
# ``McpToolingProvider.extra_skills()`` CPP seam. That move is deferred to its
# own PR because of the security-sensitive symlink-resolution and sensitive-path
# gating around it.
#
# These MUST stay path+pattern anchored, never a bare ``src/junction/agent.py``:
# the allowlist is applied as a plain ``grep -v`` to BOTH the internal-marker
# scan and the employee-alias scan, so a path-only entry would drop every line of
# this 2000+ line file from BOTH gates — a future ``code.amazon``/``brazil-build``
# /``CR-######``/alias in agent.py would then ship with CI green, exactly the
# failure mode AIM_PATTERN was added to close.
# Remove these entries when that follow-up lands.
src/junction/agent.py:.*~/\.aim
src/junction/agent.py:.*aim mcp install
src/junction/agent.py:.*AIM skills
src/junction/agent.py:.*AIM-installed
src/junction/agent.py:.*AIM install neutralized
src/junction/agent.py:.*Path.home() / "\.aim"
src/junction/agent.py:.*pkg / "\.aim"

# One more pre-existing, OSS-inert ``.aim`` read that the path-BUILDING half of
# AIM_PATTERN surfaces (the ``~/.aim`` literal alone misses it):
# slack/handler.py reads ``~/.aim/cc-plugins`` for cc-plugin agent names. It is
# the same deferred follow-up as agent.py above, allowlisted line-anchored so the
# gate still catches a NEW one.
src/junction/slack/handler.py:.*~/\.aim/cc-plugins
src/junction/slack/handler.py:.*Path.home() / "\.aim" / "cc-plugins"

# Public AWS console deep-link in the Ops Mission Control CloudWatch adapter.
# ``<region>.console.aws.amazon.com`` is the PUBLIC AWS console — the same domain
# any external AWS customer uses — and is matched only because INTERNAL_PATTERN
# carries a broad ``amazon\.com`` alternative. Allowlisted line-anchored to the
# console host so a genuinely internal ``*.amazon.com`` reference added to this
# file is still caught.
src/junction/apps/builtins/ops_mission_control/backend/providers/cloudwatch.py:.*console\.aws\.amazon\.com
# ...and its test, which asserts the console host is what a validated region produces and
# that an injected one cannot move it. Same public domain, same line-anchored narrowing.
src/junction/apps/builtins/ops_mission_control/tests/test_providers.py:.*console\.aws\.amazon\.com
